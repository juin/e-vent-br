Convenções do Projeto:

Nome da classe com mesmo nome do arquivo php. Ex: ControleUsuario.php -> class ControleUsuario

Classes de controle e modelo não têm atributos

Usar require_once para incluir arquivos

Comentar antes de todas as variáveis seguindo o seguinte modelo:
//@var inteiro
$cod_usuario

Nome de variáveis seguindo o seguinte modelo:
$cod_usuario, $data_hora_pagamento

Funções e Classes com mais de 3 linhas de conteúdo, devem ter comentário na chave de fechamento informando tal situação, por exemplo:
Class PersistenciaUsuario{

	//@var inteiro
	$cod_usuario
	
	//@var inteiro
	$cod_usuario

	//@var inteiro
	$cod_usuario
	
	public function validarAcesso($login, $senha){
        $usuario = new UsuarioSessao();
        $usuario->setCod_usuario("3");
        $usuario->setLogin("event");
        $usuario->setNome("Junior");
        $usuario->setSenha("123");
        return $usuario;
	}//Fim da função validarAcesso($login, $senha)

}//Fim da Classe PersistenciaUsuario

Ferramenta para gerar classe
http://desenvolvimentoparaweb.com/ferramentas/classesPHP/


Uso de aspas simples e duplas
$login = "Brasil";
echo $login; //Brasil
echo '$login'; //$login
echo "$login";//Brasil


Padrão -> singleton (instancia única)
http://stackoverflow.com/questions/203336/creating-the-singleton-design-pattern-in-php5